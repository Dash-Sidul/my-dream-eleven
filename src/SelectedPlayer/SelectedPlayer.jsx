import React from 'react';

const SelectedPlayer = ({chosenPlayer}) => {
    return (
        <div>
            <h1>All selected player here {chosenPlayer}</h1>
        </div>
    );
};

export default SelectedPlayer;